import React from "react";
import { View } from "react-native";

const TrackOption = () => {
  return (
    <View className="w-full inset-0 flex left-0 bottom-0 top-0 flex-1 h-full bg-trans90 absolute z-[1000]"></View>
  );
};

export default TrackOption;
